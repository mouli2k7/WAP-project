let student = {
    name : 'Ansh',
    age : 19,
    addres : 'Sonipat'
}

// let key = Object.keys(student) // {'name', 'age', 'address'}
// console.log(key)

let keyVal = Object.entries(student)
console.log(keyVal)
